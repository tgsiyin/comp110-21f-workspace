"""Some useful utility functions when working with csv files."""


def read_csv_rows(filename: str) -> list[dict[str,str]]:
    """Read the rows of a csv into a 'table'."""
    result: list[dict[str,str]] = []
    #TODO: more work!
    return result